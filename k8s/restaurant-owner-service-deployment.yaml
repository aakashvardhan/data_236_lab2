apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-owner-service
  labels:
    app: restaurant-owner-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-owner-service
  template:
    metadata:
      labels:
        app: restaurant-owner-service
    spec:
      nodeSelector:
        karpenter.sh/nodepool: general-purpose
      containers:
        - name: restaurant-owner-service
          image: 463310123657.dkr.ecr.us-east-2.amazonaws.com/restaurant-owner-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 15
            periodSeconds: 20
