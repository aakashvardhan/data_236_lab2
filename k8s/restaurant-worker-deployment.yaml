apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-worker
  labels:
    app: restaurant-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-worker
  template:
    metadata:
      labels:
        app: restaurant-worker
    spec:
      nodeSelector:
        karpenter.sh/nodepool: general-purpose
      containers:
        - name: restaurant-worker
          image: 463310123657.dkr.ecr.us-east-2.amazonaws.com/restaurant-worker:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
