apiVersion: v1
kind: Service
metadata:
  name: restaurant-service
  labels:
    app: restaurant-service
spec:
  type: ClusterIP
  selector:
    app: restaurant-service
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
